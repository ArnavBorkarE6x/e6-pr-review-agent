# =============================================================================
# e6data PR Review Agent — Local testing configuration
# =============================================================================
# These are only needed for running locally (not in GitHub Actions).
# In Actions, these come from repository secrets and workflow inputs.

# Required: one of these
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# Required for local testing
GITHUB_TOKEN=ghp_...
GITHUB_REPOSITORY=e6data/your-repo
PR_NUMBER=42

# Optional overrides
INPUT_AI_PROVIDER=anthropic
INPUT_MODEL_PRIMARY=claude-sonnet-4-5-20250929
INPUT_MODEL_LIGHTWEIGHT=claude-haiku-4-5-20251001
INPUT_MAX_FILES=50
INPUT_SKIP_DRAFTS=true
